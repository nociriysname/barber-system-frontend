import React from 'react';

// TODO: Implement modal, form and useMutation for balance top-up
export const TopUpBalance = () => {
  return (
    <button
      className="w-full text-center py-3 rounded-lg bg-button text-button-text font-semibold hover:bg-opacity-80 transition-colors active:scale-95 disabled:opacity-50"
      // onClick={() => openModal()}
      // disabled={isLoading}
    >
      Пополнить баланс
    </button>
  );
};
